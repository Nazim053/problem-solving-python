from math import *
a,b,c = float(input()), float(input()),float(input())

d = (pow(b,2)-(4*a*c))

if d == 0:
  print (-b / (2* a) )
elif d > 0:
  root1 = ((-b - sqrt(d)) / (2 * a))
  root2 = ((-b + sqrt(d)) / (2 * a))
  print(min(root1, root2), max(root1, root2), sep='\n')
else:
  print('Нет корней')
